G0_VERDICT: GO

The v3 canonical contract is internally consistent, mathematically executable, and sufficiently frozen for local implementation to begin. This GO is deliberately narrow: it authorizes only local source implementation and CPU/mock tests. It does not authorize any GPU, data, producer, capacity, training, falsifier, real selected-checkpoint merge/trace, or latency work.

## Artifact verification

All supplied canonical identities match exactly:

| Artifact | Verified SHA256 |
|---|---|
| `FINAL_PROPOSAL.md` | `c4f091723a3deaf14d97692b1eea8219872dd911b0b3efa73db65f3e79d75ffe` |
| `EXPERIMENT_PLAN.md` | `b9860ad88af6ad9e0429bdf9e9c61fd9b5a50d56d41af23e5f2c767fa448c3a3` |
| `PROSPECTIVE_V2_CONTRACT.md` | `6761fba7ad499b7a373205dc549f2056b9bee042367cb5282b19d7639ef48570` |
| `PROSPECTIVE_V2_CONTRACT.json` | `5392ef64b4fbf52d4abf1dbbc351477c7e8ba0333aac8a3531f91f1699ffa867` |

`EXECUTION_CORRECTIONS_20260806_154137.md` and the complete v2 raw review were also read. The JSON supersession hashes correctly identify the v2 proposal, plan, Markdown, JSON, and review that v3 replaces.

## Required v3 confirmations

### 1. G8 deployment authorization is now complete and monotonic

Confirmed across Proposal, Plan, Markdown, and JSON:

- G8 requires `C1-EFFICACY PASS` plus the frozen D-seed0 `selected_feasible` identity.
- G8 first authorizes exactly one real selected-checkpoint merge/wrapper-removal/trace/dtype/output-exactness audit.
- Latency remains closed unless that first G8 substage passes.
- Only then does G8 conditionally open the single fixed latency fixture.
- Final C1-SYSTEM/DEPLOYMENT requires both receipts.

This removes the v2 authorization gap. No merge/trace receipt is required before the gate that authorizes its production, and G7 remains limited to the one common falsifier opening plus C1-EFFICACY/C2 adjudication.

### 2. Restart-level latency inference is fully executable

Confirmed identically across the canonical artifacts:

```text
r_j = median of 50 paired log(merged/released) ratios in restart j
estimate = arithmetic mean of the 20 r_j values
s = sample standard deviation of r_j with ddof=1
90% CI = estimate ± t_(0.95,19) * s / sqrt(20)
PASS iff both endpoints are strictly inside [log(.98), log(1.02)]
```

The workload is also frozen: first 50 checkpoint prompts by stable manifest rank, end-to-end speculative generation, `max_new_tokens=64`, exact target output, 20 independent restarts, 200 cycling warmups, 50 alternating released/merged pairs, and seconds per emitted token. The point estimator, dispersion, degrees of freedom, quantile, endpoint strictness, and TOST alpha `.05` leave no result-dependent choice.

## Final cross-file and mathematical audit

| Category | Frozen value | Verdict |
|---|---|---|
| Route | prospective-v2 FBPF-DFlash; formal refine status `REVISE 8.77` | Consistent; G0 does not relabel it READY |
| Model | Qwen3-4B target; released Qwen3-4B-DFlash-b16; SDPA | Consistent with local config |
| Geometry | b16/L15; five draft layers; hidden 2560; intermediate 9728; q/kv 32/8; target layers `[1,9,17,25,33]` | Consistent |
| D-PACE | pinned commit `f36bad6e6b0f9f5b59e1e6cf405c705b46d2b43f`; alpha `.5`; full 15-position coverage | Formula and detached weights match local source |
| Tensor reduction | one source prompt → four physical rows; one anchor/row; `/bsz=/N=4`; 8,000 steps | Exact parity route is executable |
| Gold | frozen-target-only autoregressive greedy continuation; float32 decisions; lowest-ID ties | Correct verifier-defined truth, never comparator prediction |
| Anchors | Python-rounded offsets from `T-16`; minimum T=19; exactly four complete blocks | Consistent |
| LoRA | layers 3/4 q/k/v/o/gate/up/down; r=16; alpha=16; 1,835,008 parameters | Paths and count correct |
| LoRA numerics | float32 masters/moments/branch; isolated seed; zero B; bf16 stored merge | Implementable; zero/merge invariants are gated |
| Constraints | epsilon `1e-4`; tau-f `1e-5`; margin `9e-5`; block maxima; at most four detached tie-averaged VJP rows | Feasible-set equivalence is exact |
| Transactions | four projection sweeps; eight restorations; exact candidates; restore+commit/skip/rollback semantics | Complete and testable |
| Arms | A DPACE; B STATIC-PF; C DYNAMIC-U; D FBPF | Matched and causally interpretable |
| Training | 8,000 fit; 1,000 checkpoint; seeds 0/1/2; checkpoints 1k…8k | Consistent |
| Identities | five definitions; released plus 12 trained instances; selected/diagnostic labels | Opening consequences are deterministic |
| Data | fixed domain quotas, NFKC/tokenization/Jaccard graph, cross-domain exclusion, component-atomic allocator | Deterministic and independently replayable |
| Rank bytes | UTF-8, lowercase hex, no-leading-zero decimal, single NUL separators | Fully pinned |
| Reserves | per-domain/split `max(64,ceil(.10*quota))`; permanent consumption; explicit exhaustion failure | Feasible or terminal without manual rescue |
| Estimand | equal blocks within prompt, matched-seed prompt mean, equal prompts | Prompt-balanced and unambiguous |
| Bootstrap | 10,000 component-cluster/domain-stratified replicates; seed 2026080601; linear percentile quantiles | Executable |
| Power | explicit design effect, effects, sidedness, precision formulas, and maximum `n_power`; no seed discount | Executable and conservative |
| Claims | C1-EFFICACY/C2 at G7; ordered C1-SYSTEM/DEPLOYMENT at G8 | Non-circular |
| Cost | three A/D pairs; 20+200; every D step `K=4`; honest A work; Q95 ratio definition; memory gates | Complete |
| Compute | 68–193 A800-hours | Consistent with milestone sums |
| Ladder | unified G0–G8 with one opening and permanent no-rescue rules | Consistent and monotonic |

No unresolved frozen-constant mismatch or mathematical blocker remains. The exact nonlinear all-position checks continue to guard block-max ties and active-position switches; constraint sampling/truncation remains forbidden.

## Route isolation

R083 remains `CLOSED_OPERATIONAL_FAILURE_EXIT1_OPENING_CONSUMED_NO_RETRY_JOB10141601`. Old validation, reserved, formal, and R083 outcomes may not be read, reconstructed, or scored. The only future old-100k uses are the frozen normalized-hash/8-gram exclusion index and aggregate count/cluster/paired-SD-or-ICC upper bounds in the G3 POWER_RECEIPT. No artifact or claim may call prospective-v2 a retry, rescue, repair, or downstream route.

## What this GO opens

G0 now opens only:

- local source implementation of the frozen contract;
- CPU analytic tests and CPU/mock-model tests.

## What remains closed

The following remain explicitly unauthorized until their later gates pass:

- every GPU operation, including the one synthetic GPU smoke;
- all A/D throughput and memory pairs;
- producer POWER_RECEIPT access or generation;
- prospective source reading for split/materialization and every data-generation operation;
- target continuation/sequence generation;
- 32/512-block capacity work;
- all 4×3 training and checkpoint selection;
- the common falsifier opening, generation, evaluation, bootstrap, and claim adjudication;
- the real selected-checkpoint merge/wrapper-removal/trace/dtype/output audit;
- deployment latency;
- every prohibited R083 artifact or outcome.

G1 requires the full CPU unit/parity suite and a fresh code-review GO before it may open exactly one synthetic GPU smoke. This G0 verdict grants no authority beyond local implementation and CPU/mock tests.
