# PCLD003 P0 teacher-geometry diagnosis

Status: implementation/numerical correction under independent re-review; P0 not yet passed.

- P0 job `10168382` stopped on the first record because the original manual recurrence gate required elementwise hidden `allclose`; observed max absolute difference was `2.0`.
- Diagnostic jobs: `10168386` (6 records) and `10168391` (frozen smoke32, 32 records / 512 rows).
- Full diagnostic artifact: `artifacts/analysis/pcld_teacher_geometry_10168391.json`.

Full smoke32 observations:

- row identity by all-pairs hidden cosine best match: `512/512` diagonal;
- hidden relative RMSE: max `0.0325547084`;
- hidden cosine: min `0.9994702339`;
- raw batched/manual BF16 top1 agreement: `510/512`;
- both disagreements have batched top1-top2 margin exactly `0.0` and manual margin `0.25`;
- centered-logit epsilon calibrated only from the 510 agreeing rows: `1.25`, so both disagreements are inside the conservative `2*epsilon=2.5` tie region;
- stable non-tie disagreements: `0`.

The frozen method states that raw hidden RMSE is reported and is not an independent GO gate. The proposed correction therefore keeps full hidden diagnostics, hard-gates exact row mapping and stable non-tie token parity, reports ambiguous/raw disagreements separately, and retains explicit row0/row15 receipts. No architecture, data, objective, efficacy threshold, or online path changed.
